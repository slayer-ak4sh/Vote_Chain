{
  "name": "simple-blockchain-project",
  "version": "1.0.0",
  "description": "A simple blockchain project with Solidity smart contracts",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost && node frontend/update-addresses.js",
    "frontend": "node frontend/server.js",
    "dev": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}